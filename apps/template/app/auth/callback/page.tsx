export default function AuthCallbackPage() {
  return (
    <main>
      <h1>Auth Callback</h1>
      <p>
        This route handles Supabase auth callbacks (magic links / OAuth). Implement your redirect
        and session handling here.
      </p>
    </main>
  );
}
